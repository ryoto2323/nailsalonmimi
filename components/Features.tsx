// Fix: Stubbing out unused component to resolve build errors (references missing types/constants and incorrect domain content).
import React from 'react';

const Features: React.FC = () => {
  return null;
};

export default Features;